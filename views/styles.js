const styles = theme => ({
  buttonMargin: {
    margin: theme.spacing.unit,
  },
  titleContainer: {
    padding: '20px',
  },
  calendarTitle: {
    marginLeft: `${theme.spacing.unit * 2}px`,
  },
  rotaScroller: {
    width: '100%', 
    overflow: 'scroll' 
  },
});

export default styles;
