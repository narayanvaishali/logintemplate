const styles = theme => ({
  buttonMargin: {
    margin: theme.spacing(1),
  },
  titleContainer: {
    padding: '20px',
  },
  calendarTitle: {
    marginLeft: `${theme.spacing(2)}px`,
  },
  rotaScroller: {
    width: '100%', 
    overflow: 'scroll' 
  },
});

export default styles;
